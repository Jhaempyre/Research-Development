export  const DB_NAME = 'research-development'

/*// Define the function to extract mobile number from an object
function getMobileNumber(req) {
    // Destructure the 'mobile' property from the object
    const { mobile } = req;

    // Return the value of 'mobile'
    return mobile;
}

// Define the data object
const data = {
    mobile: '123-456-7890',
    // other properties...
};

// Call the function to extract the mobile number
const mobileNumber = getMobileNumber(data);
console.log(mobileNumber); // Output: 123-456-7890

// Export the function if you're using ES6 module syntax
export default mobileNumber;
*/

